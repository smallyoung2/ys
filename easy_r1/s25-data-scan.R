# 데이터 입출력
#scan():키보드 입력(CLI)
#edit():데이터 프레임 입력(GUI)

# 키보드입력
# 아무것도 입력하지 않고 enter치면 입력 종료
#숫자입력
num=scan()    #벡터
num

sum(num)
mode(num)

#문자입력
name <- scan(what=character())
name
mode(name)

#GUI편집기 : edit() 함수
df <- data.frame()
df <- edit(df)
df
